NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Public site URL for the marketing page's canonical link, Open Graph image,
# and sitemap.xml. Set this to your real deployed domain in production —
# without it, those fall back to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3000
