# ============================================================
#  Secrets — never commit
# ============================================================
.env
.env.*
!.env.example
infra/.env
# Plaintext VPS credentials — never let this reach git history.
vps.md

# ============================================================
#  Private / internal-only (kept out of the public repo)
# ============================================================
/private/

# ============================================================
#  Databases & runtime state (contain user data / secrets)
# ============================================================
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
*.db
runtime_state.json
backend/runtime_state.json
memory.sqlite3
# Dev compose writes SQLite + runtime state into the bind-mounted source tree.
backend/state/

# ============================================================
#  Node / Next.js
# ============================================================
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ============================================================
#  Python
# ============================================================
__pycache__/
*.py[cod]
*.pyo
*.pyd
env/
venv/
.venv/
.pytest_cache/

# ============================================================
#  Build / packaging artifacts (regenerated by the build)
# ============================================================
dist/
build/
desktop/dist/
desktop/resources/backend-runner
desktop/resources/backend-runner.exe
desktop/resources/frontend/

# ============================================================
#  Certificates / keys
# ============================================================
*.pem
*.key
*.crt
*.pfx

# ============================================================
#  Logs / OS / editor / junk
# ============================================================
*.log
logs/
.vscode/
.idea/
.DS_Store
Thumbs.db
# Personal Claude Code permission overrides — not shared project config.
.claude/settings.local.json
*.swp
$null
**/$null

# Built installers dropped in for Caddy to serve (see infra/README.md).
infra/downloads/*
!infra/downloads/.gitkeep
